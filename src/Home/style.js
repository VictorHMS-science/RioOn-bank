import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#820AD1', // Cor de fundo roxa do Nubank
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#FFFFFF', // Branco
    marginBottom: 10,
  },
  subtitle: {
    fontSize: 18,
    color: '#FFFFFF', // Branco
  },
});

export default styles;